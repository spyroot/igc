# Extra Python deps installed on top of the NGC PyTorch base image, which already
# provides CUDA + a torch build tuned for GB300 / Blackwell (aarch64 Grace).
# Do NOT pin torch/torchvision here — use the base image's. See docker/Dockerfile.train.
transformers>=4.40
accelerate>=0.30
peft>=0.11
datasets
evaluate
tokenizers
safetensors
sentencepiece
# RL env
gymnasium>=1.0,<2
gymnasium
# mock REST env (igc/envs/rest_mock_server.py)
Flask
flask_restful
flask_httpauth
StringGenerator
aniso8601
# logging / metrics / experiment tracking
tensorboard
wandb
weave
loguru
tabulate
prettytable
scikit-learn
nltk
rouge_score
# distributed launcher (often already in NGC; harmless if reinstalled)
deepspeed
